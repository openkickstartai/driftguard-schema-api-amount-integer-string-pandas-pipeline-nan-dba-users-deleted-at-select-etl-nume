name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -r requirements.txt
      - run: pytest test_driftguard.py -v --tb=short
      - name: Smoke test CLI
        run: |
          echo '[{"id":1,"name":"test"}]' > sample.json
          python main.py snapshot sample.json
          python main.py check sample.json
